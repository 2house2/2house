default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: to_house_app_development

test:
  <<: *default
  database: to_house_app_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
